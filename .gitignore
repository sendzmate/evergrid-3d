# macOS
.DS_Store

# Source assets not needed to run the deployed site.
# The site renders from the optimized web/ folder (WebP frames).
*.mp4
Image References/

# The heavy original 1080p JPG frame set (164MB) is NOT deployed —
# the site uses web/ instead. Keep it out of the repo.
frames/

# temp check artifact
web/_chk134.jpg
